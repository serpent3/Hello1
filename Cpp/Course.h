#include "headers.h"
using namespace std;

class Course
{
private:
    //array<Student, 3> ar_Students = a_Students;
    //Teacher Teacher1;
public:
    Course(array<Student, 3> &a_Students, Teacher *Teacher1);
    ~Course();
    // void addStudents;
    // void addTeacher;
};
